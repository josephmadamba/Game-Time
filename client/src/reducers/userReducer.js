const userReducer = ( state = {user: {}},action) => {
    let newState = Object.assign({}, state);

    switch(action.type){
        default:
            return newState
    }



}


export default userReducer